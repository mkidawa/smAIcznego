---
import Layout from "@/layouts/Layout.astro";
import DietGenerateView from "../../modules/diets/generation/components/DietGenerateView";

const acceptedTerms = Astro.locals.user?.profile?.terms_accepted;

if (!acceptedTerms) {
  return Astro.redirect("/profile");
}
---

<Layout>
  <DietGenerateView client:load />
</Layout>
